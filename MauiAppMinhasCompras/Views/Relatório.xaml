<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppMinhasCompras.Views.Relatório"
             Title="Relatório">
    <VerticalStackLayout Padding="20">
        <Label Text="Filtrar por Período" FontAttributes="Bold" />

        <DatePicker x:Name="DataInicioPicker" />
        <DatePicker x:Name="DataFimPicker" />

        <Button Text="Filtrar" Clicked="OnFiltrarClicked" />
        
        <CollectionView x:Name="ProdutosCollectionView">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout Padding="10">
                        <Label Text="{Binding Name}" FontAttributes="Bold" />
                        <Label Text="{Binding Price, StringFormat='{}{0:C}'}" />
                        <Label Text="{Binding DataCadastro, StringFormat='{}{0:MM/dd/yyyy}'}" />
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>